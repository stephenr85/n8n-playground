# n8n-playground

Exploring n8n for workflow orchestration and AI agent workflows.

## Setup

```bash
cp .env.example .env
docker compose up -d
```

n8n will be available at [localhost:5678](http://localhost:5678).

## Workflows

Exported workflow JSON lives in `workflows/`. Import via n8n UI or API.
